import{Alert,ScrollView,Text,View}from'react-native';import * as DocumentPicker from'expo-document-picker';import * as Sharing from'expo-sharing';import{File,Paths}from'expo-file-system';import{Button,s}from'@/components/UI';import{cancelTaskNotification,scheduleTaskNotification}from'@/services/notifications';import{exportData,importData,listTasks}from'@/services/database';import{useTasks}from'@/context/TaskContext';
export default function Settings(){const{tasks,refresh}=useTasks();const backup=async()=>{try{const file=new File(Paths.cache,`voice-work-backup-${new Date().toISOString().slice(0,10)}.json`);file.create({overwrite:true});file.write(JSON.stringify(await exportData(),null,2));if(await Sharing.isAvailableAsync())await Sharing.shareAsync(file.uri,{mimeType:'application/json',dialogTitle:'업무 데이터 백업 저장'});else Alert.alert('백업 완료',file.uri)}catch(e){Alert.alert('백업 실패',e instanceof Error?e.message:'파일을 만들 수 없습니다.')}};const choose=async()=>{const result=await DocumentPicker.getDocumentAsync({type:'application/json',copyToCacheDirectory:true});if(result.canceled)return;try{const raw=await new File(result.assets[0].uri).text();const data=JSON.parse(raw);Alert.alert('데이터 복원','기존 데이터에 추가할까요, 전체 교체할까요?',[{text:'취소'},{text:'추가',onPress:()=>restore(data,'merge')},{text:'전체 교체',style:'destructive',onPress:()=>restore(data,'replace')}])}catch{Alert.alert('잘못된 파일','유효한 JSON 백업 파일이 아닙니다.')}};const restore=async(data:any,mode:'merge'|'replace')=>{try{for(const t of tasks)await cancelTaskNotification(t);await importData(data,mode);const restored=await listTasks();for(const t of restored)await scheduleTaskNotification(t);await refresh();Alert.alert('복원 완료',`${restored.length}개의 일정을 불러왔습니다.`)}catch(e){Alert.alert('복원 실패',e instanceof Error?e.message:'파일 형식이 올바르지 않습니다.')}};return <ScrollView style={s.screen} contentContainerStyle={s.content}><View style={[s.card,{gap:8,borderColor:'#F59E0B'}]}><Text style={s.h2}>안전 안내</Text><Text style={s.body}>운전 중에는 휴대폰 화면을 직접 조작하지 말고, 반드시 안전한 장소에 정차한 후 사용하세요.</Text></View><View style={[s.card,{gap:12}]}><Text style={s.h2}>수동 백업</Text><Text style={s.muted}>일정과 메모를 JSON 파일로 저장하거나 다른 앱으로 공유합니다. 앱 삭제 전에는 반드시 백업하세요.</Text><Button title="JSON 백업 내보내기" onPress={backup}/></View><View style={[s.card,{gap:12}]}><Text style={s.h2}>데이터 복원</Text><Text style={s.muted}>이 앱에서 만든 JSON 파일을 선택하세요. 기존 데이터에 추가하거나 모두 교체할 수 있으며, 알림도 다시 예약됩니다.</Text><Button title="JSON 백업 파일 선택" variant="secondary" onPress={choose}/></View><View style={s.card}><Text style={s.h2}>개인용 오프라인 앱</Text><Text style={s.muted}>서버, 계정, API 키, 클라우드 동기화를 사용하지 않습니다. 음성 인식은 휴대폰에 설치된 음성 인식 서비스에 따라 인터넷을 사용할 수 있습니다.</Text></View></ScrollView>}
